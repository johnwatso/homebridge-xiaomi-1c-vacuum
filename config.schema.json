{
  "pluginAlias": "OneCMatter",
  "pluginType": "platform",
  "headerDisplay": "1C Matter - Xiaomi 1C Vacuum (Matter Native - Local LAN)",
  "schema": {
    "type": "object",
    "properties": {
      "ip": {
        "title": "Device IP Address",
        "type": "string",
        "required": true,
        "description": "The local IP address of your 1C vacuum (e.g., 192.168.1.50)."
      },
      "token": {
        "title": "Device Token",
        "type": "string",
        "required": true,
        "description": "The 32-character local token for your vacuum."
      },
      "deviceId": {
        "title": "Device ID (DID)",
        "type": "string",
        "required": true,
        "description": "The numeric Device ID of your vacuum (needed for local MIoT commands)."
      },
      "pollInterval": {
        "title": "Polling Interval (seconds)",
        "type": "integer",
        "default": 30,
        "minimum": 10
      }
    }
  }
}
